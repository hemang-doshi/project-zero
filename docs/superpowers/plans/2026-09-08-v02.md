# Project Zero v0.2 Implementation Plan

Required workflow: superpowers:executing-plans, one agent in this task.
Spec: ../specs/2026-09-08-v02-design.md and PROJECT_ZERO_SPEC.md.

## Constraints
Preserve current physical soak and live runtime; isolated development data.
Use existing working-checkout preference, separate build/v0.2 branch, and avoid
rebuilding live binaries. Do not touch work devices or discover repositories.

## Task checklist
- [x] Save approved design and implementation checklist.
- [ ] 1. Close v0.1 gates and prove supported desktop Codex observation.
  - [ ] Review soak, interruption, protocol, trace and performance evidence.
  - [x] Repair standalone Codex executable and pin verified version.
  - [ ] Read-only probe of owning service: project/task identity, lifecycle,
        terminal outcome, reconnect and deduplication. Do not resume tasks.
  - [ ] Record actual compatibility or exact blocker before dependent tasks.
- [ ] 2. Projects, durable session identity/end, context provenance/overrides,
      versioned migration, CLI and projection rebuild.
- [ ] 3. Scoped bounded Git, Codex and native Spotify adapters; freshness and
      outage isolation; 15-second Git and 2-second Spotify polling.
- [ ] 4. Local intent templates and review; explicitly approved restricted
      Codex parsing; daily/concurrency/deadline limits and honest usage.
- [ ] 5. Native menu bar, hybrid desk schema and backward compatibility;
      flash only after v0.1 evidence and soak.
- [ ] 6. Three typed auditable automations, immediate disable and feedback;
      14-day evaluation with reviewed incorrect-firing rate below 5%.

For each implementation task: write a meaningful boundary test, observe its
failure, implement the smallest coherent behavior, run focused and necessary
regression checks, record evidence, commit. Preserve stable schemas, dry-run,
authorization and causal trace at all new interfaces. No milestone is complete
on simulated evidence when real hardware or desktop observation is required.

## Current execution status

Gate 1 is blocked: the supported desktop task-owning service attachment endpoint
is unavailable, and the v0.1 soak is still running. See
[prerequisite evidence](../../evidence/2026-09-08-v02-prerequisites.md).
Dependent features have not started.
